extends layout
   
block content
  if superuser
    .base_page
        .pad_mod
            a.btn.btn-primary.space(role='button' href='http://localhost:3021/recipes/add') Añadir Receta
  .base_page
    .pad_mod 
      h1  Listado de recetas
      p 
      - let pos=0
        each recipe in recipes
          .card 
            .card-conten 
              - pos++
              .card-img 
                //div(class="fakeimg2") #{recipe.img}
                img(src=`/images/${recipe.img}` alt=`${recipe.img}` class="img")
              .card-text   
                p
          
                  b No.:
                  | #{pos}
                p
                  b Titulo: 
                  |  #{recipe.title}
                p 
                b Ingrediente Principal
                each ingredient in recipe.ingredients
                  p
                    | &dot;  #{ingredient}
                  p
                p  
            b Problemas Relacionados
              each issue in recipe.relatedIssues
                p
                  | &dot;  #{issue} 
                p     
              a.btn.btn-primary.px-4.float-end.mt-4(role='button' href='/recipes/read/'+recipe._id) Ver Mas
          p